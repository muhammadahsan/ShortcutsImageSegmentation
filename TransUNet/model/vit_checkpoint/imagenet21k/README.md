# TransUNet pretrained ViT weights

Place `R50+ViT-B_16.npz` here for ImageNet-21k pretraining (recommended).

See [Beckschen/TransUNet](https://github.com/Beckschen/TransUNet) — if the official Google Drive link has expired, use the copy linked in the repo README.

Training still runs without this file (random init for decoder + ViT).
